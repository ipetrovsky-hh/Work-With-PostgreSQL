-- create
CREATE TABLE EMPLOYEE (
  Id INTEGER PRIMARY KEY,
  Code TEXT NOT NULL,
  Name TEXT NOT NULL,
  StatusId TEXT NOT NULL
);

-- insert
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(1,'gargadgadfga','Запрос предложений 1','45');
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(2,'bsftrggdfgadfgdfat','Запрос предложений 2','2');
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(3,'gfadgdfsgdfsgs','Запрос предложений 3','45');
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(4,'afgereaerffdgvdf','Запрос предложений 4','3');
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(5,'dgadfterdsgsdgad','Запрос предложений 5','45');
INSERT INTO EMPLOYEE(Id,Code,Name,StatusId) VALUES(6,'argrgag','Запрос предложений 6','2');


-- fetch 
--SELECT xmlcomment(EMPLOYEE.Id::text) AS ID,Code,Name,StatusId FROM EMPLOYEE
SELECT '<?xml version="1.1"?>' AS "XML_OUTPUT "
UNION ALL
SELECT CONCAT('<ROW>',
xmlforest(EMPLOYEE.Id::text AS ID, 
EMPLOYEE.CODE::text AS CODE,
EMPLOYEE.Name::text AS Name,
EMPLOYEE.StatusId::text AS StatusId),
'</ROW>') FROM EMPLOYEE
